type Video {
  id: ID!
  type: String!
  title: String!
  slug: String!
  content: String
}

input VideoUpdateContentInput {
  id: ID!
  content: String
}

type Mutation {
  videoUpdateContent(input: VideoUpdateContentInput!): Int
}
