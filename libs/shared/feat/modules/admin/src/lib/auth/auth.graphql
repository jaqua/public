input UserInput {
  username: String!
  password: String!
}

type AccessToken {
  access_token: String!
}

# type Query {
#   getUsers: [User]
#   getUser(param: UserParam): User
# }

type Mutation {
  login(input: UserInput!): AccessToken
}
